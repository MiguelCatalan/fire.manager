class Firefighter:

    def __init__(self, firefighter_id: str, email: str, name: str, phone: str):
        self.firefighter_id = str(firefighter_id)
        self.email = str(email)
        self.name = str(name)
        self.phone = str(phone)
